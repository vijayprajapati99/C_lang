#include <stdio.h>

int main()
{
    int radius = 7;
    int pi = 3.14;
    int height = 78;
    float volume = pi * radius * radius * height;
    printf("the volume of the cylinder is %f", volume);
    return 0;
}